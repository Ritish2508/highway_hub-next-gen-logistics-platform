version: "3.9"
services:
  devops-agent:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ~/.kube:/root/.kube:ro
    networks:
      - highwayhub_net

networks:
  highwayhub_net:
    external: true