apiVersion: v1
kind: Service
metadata:
  name: devops-agent
  namespace: default
spec:
  selector:
    app: devops-agent
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP